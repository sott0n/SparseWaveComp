[project]
name = "sparsewave-pytorch-examples"
version = "0.1.0"
description = "PyTorch export examples for SparseWave"
requires-python = ">=3.10,<3.15"
dependencies = [
  "numpy>=2.0,<3",
  "torch==2.13.0",
]

[tool.uv]
package = false

[tool.uv.sources]
torch = {index = "pytorch-cpu"}

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true
